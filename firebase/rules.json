{
    "rules": {
        "calendar": {
            ".read": true,
            ".write": "auth != null && auth.token.admin === true"
        },
        "db-info": {
            "monitoring": {
                ".read": true,
                ".write": "auth != null && auth.token.admin === true"
            },
            "pulses": {
                ".read": "auth != null && auth.token.admin === true",
                ".write": "auth != null && auth.token.admin === true"
            },
            "statuses": {
                ".read": true,
                ".write": "auth != null && auth.token.admin === true"
            }
        },
        "field-information": {
            ".read": true,
            ".write": "auth != null && auth.token.admin === true"
        },
        "scores": {
            ".read": true,
            ".write": "auth != null && auth.token.admin === true"
        }
    }
}
